name: "Zeek Ransomware Response"
version: 1.0
description: >
  Response for ransomware detected by Zeek NSM.
  Triggers immediate network isolation via OpenDaylight.

trigger:
  event_type: "alerts.network.zeek"
  condition: "{{ trigger.payload.alert_type contains 'ransomware' }}"

steps:
  - name: "Log Zeek Ransomware Event"
    action:
      type: "PUBLISH_EVENT"
      event:
        type: "RANSOMWARE_DETECTED"
        data:
          event_id: "{{ trigger.event_id }}"
          source_ip: "{{ trigger.payload.source_ip }}"
          severity: "{{ trigger.payload.severity }}"
          alert_type: "{{ trigger.payload.alert_type }}"
          signature: "{{ trigger.payload.signature }}"
          note_type: "{{ trigger.payload.note_type }}"
          timestamp: "{{ trigger.timestamp }}"
          
  - name: "Initiate Host Network Isolation (ODL)"
    action:
      type: "PUBLISH_EVENT"
      event:
        type: "INITIATE_MITIGATION"
        data:
          targetHost: "{{ trigger.payload.source_ip }}"
          action: "ISOLATE_VLAN"
          sdn_controller: "opendaylight"
          priority: "high"
          justification: "Zeek NSM detected ransomware activity from {{ trigger.payload.source_ip }} (Note: {{ trigger.payload.note_type }}). Isolating."
          source_event_id: "{{ trigger.event_id }}"

  - name: "Alert SOC"
    action:
      type: "PUBLISH_EVENT"
      event:
        type: "SOC_ALERT"
        data:
          alert_level: "critical"
          incident_type: "network_ransomware_detection"
          affected_host: "{{ trigger.payload.source_ip }}"
          severity: "{{ trigger.payload.severity }}"
          action_taken: "HOST_ISOLATED"
          requires_investigation: true
          message: "CRITICAL: Zeek NSM detected ransomware activity from {{ trigger.payload.source_ip }}. Automatically isolated via SDN."
