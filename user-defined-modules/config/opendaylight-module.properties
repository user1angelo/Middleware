# OpenDaylightModule configuration

# RabbitMQ / Message Bus
rabbitmq.host=localhost
rabbitmq.port=5672
rabbitmq.user=user
rabbitmq.password=password
rabbitmq.workflow_queue=workflow_queue
rabbitmq.command_queue=odl_commands_queue

listeners.tcp.default = 5672         
listeners.ssl.default = 5671          

ssl_options.cacertfile = /etc/rabbitmq/ca_cert.pem     
ssl_options.certfile   = /etc/rabbitmq/server_cert.pem
ssl_options.keyfile    = /etc/rabbitmq/server_key.pem

# Strongly recommended for mutual TLS and proper verification:
ssl_options.verify = verify_peer
ssl_options.fail_if_no_peer_cert = true

# OpenDaylight RESTCONF
odl.base_url=http://localhost:8181
odl.username=admin
odl.password=admin

# Quarantine policy defaults (can be overridden per mitigation payload)
mitigation.policy.mode=strict_bi_directional
mitigation.containment.arp=true
mitigation.containment.dhcp=true

# Module identity
# IMPORTANT: module.id must match the config filename (without .properties)
# so the web dashboard can correlate DB records with this config.
module.id=opendaylight-module
module.name=OpenDaylight SDN Module
module.type=sdn_controller
